## Configuracion de elasticsearch
elasticsearch:
  host: ${ELASTICSEARCH_HOST:localhost}
  port: 9200
  credentials:
    user: ${ELASTICSEARCH_USER:f1ehsxr3ii}
    password: ${ELASTICSEARCH_PWD:a9toyqimvu}

logging:
  level:
    tracer: TRACE

## Configuracion de Spring
spring:
  application:
    name: ms-inventory-products
  datasource:
    url: jdbc:h2:mem:productsbackenddb
  jpa:
    show-sql: true
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true
  data:
    elasticsearch:
      cluster-nodes: localhost:9200
      cluster-name: products-cluster

## Configuracion de Instancia 
server:
  port: 8088

## Configuracion del cliente de Eureka
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true
    hostname: ms-inventory-products
